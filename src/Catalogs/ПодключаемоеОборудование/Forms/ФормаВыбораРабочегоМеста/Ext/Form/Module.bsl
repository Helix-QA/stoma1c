
#Область ОбработчикиСобытийФормы

&НаСервере
// Процедура, обработчик события ПриСозданииНаСервере
//
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	РабочееМесто = МенеджерОборудованияВызовСервера.ПолучитьРабочееМестоКлиента();

	ОткрыватьПриПервомОбращении = Параметры.ОткрыватьПриПервомОбращении;
	ИмяКомпьютера = Параметры.ИмяКомпьютера;

	// Если передали имя компьютера, то значит необходимо выбрать рабочее место
	// из уже существующего (не пустого) списка рабочих мест
	Если Не ПустаяСтрока(ИмяКомпьютера) Тогда
		НовыйМассив = Новый Массив();
		НовыйМассив.Добавить(Новый ПараметрВыбора("Отбор.ПометкаУдаления", Ложь));
		НовыйМассив.Добавить(Новый ПараметрВыбора("Отбор.Код", ИмяКомпьютера));
		НовыйФиксированныйМассив = Новый ФиксированныйМассив(НовыйМассив);
		Элементы.РабочееМесто.ПараметрыВыбора = НовыйФиксированныйМассив;

		Элементы.НадписьНастройки.Доступность = Ложь;
		Элементы.ПодключитьИНастроитьВО.Доступность = Ложь;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
// Процедура, обработчик команды ПодключитьИНастроитьВО
//
Процедура ПодключитьИНастроитьВО(Команда)
	
	ОткрытьФорму("Справочник.ПодключаемоеОборудование.ФормаСписка",,,,,,Новый ОписаниеОповещения("ПодключитьИНастроитьВОЗавершение", ЭтотОбъект), РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);	

КонецПроцедуры

&НаКлиенте
// Процедура, обработчик команды ВыбратьИЗакрыть
//
Процедура ВыбратьИЗакрыть(Команда)
	
	Если ЗначениеЗаполнено(РабочееМесто) Тогда
		
		Параметры.РабочееМесто = РабочееМесто;
		
		СписокНастроек = Новый СписокЗначений();
		СписокНастроек.Добавить(ОткрыватьПриПервомОбращении, "ОткрытьФормуВыбораРМПриПервомОбращении");
		МенеджерОборудованияВызовСервера.СохранитьПользовательскиеНастройкиВнешнегоОборудования(СписокНастроек);
		
		ОчиститьСообщения();
		СтруктураВозврата = Новый Структура("РабочееМесто, Ответ", РабочееМесто, КодВозвратаДиалога.ОК);
		Закрыть(СтруктураВозврата);
	Иначе
		ОчиститьСообщения();
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru='Выберите рабочее место'"), РабочееМесто, "РабочееМесто");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
// Процедура, обработчик события "ПодключитьИНастроитьВОЗавершение"
//
Процедура ПодключитьИНастроитьВОЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если Не ЗначениеЗаполнено(РабочееМесто) Тогда
		РабочееМесто = МенеджерОборудованияКлиентПовтИсп.ПолучитьРабочееМестоКлиента();
	КонецЕсли;

КонецПроцедуры

#КонецОбласти