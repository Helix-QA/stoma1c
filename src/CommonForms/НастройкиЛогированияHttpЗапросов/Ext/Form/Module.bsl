 
#Область ОбработчикиСобытийФормы

&НаСервере
// Процедура, обработчик события "ПриСозданииНаСервере".
//
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	РежимЛогирования = Константы.ЛогироватьHTTP.Получить();
	
	ЭтотОбъект.РежимРаботы = ?(ТипЗнч(РежимЛогирования) = Тип("Число"), РежимЛогирования, 0);

КонецПроцедуры


#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
// Процедура, обработчик события команды "Установить".
//
Процедура Установить(Команда)
	
	УстановитьНаСервере();
	
	ЭтаФорма.Закрыть();
	
КонецПроцедуры

&НаСервере
// Процедура, обработчик события команды "Установить" на сервере.
//
Процедура УстановитьНаСервере()

	Константы.ЛогироватьHTTP.Установить(ЭтотОбъект.РежимРаботы);	
	
КонецПроцедуры

#КонецОбласти