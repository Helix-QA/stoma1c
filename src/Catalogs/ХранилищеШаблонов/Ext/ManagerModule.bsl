
#Область СлужебныйПрограммныйИнтерфейс

// Функция выполняет получение имени поля из доступных полей компоновки данных.
//
Функция ПолучитьИмяПоляВШаблоне(Знач ИмяПоля) Экспорт
	
	ИмяПоля = СтрЗаменить(ИмяПоля, ".DeletionMark", ".ПометкаУдаления");
	ИмяПоля = СтрЗаменить(ИмяПоля, ".Owner", ".Владелец");
	ИмяПоля = СтрЗаменить(ИмяПоля, ".Code", ".Код");
	ИмяПоля = СтрЗаменить(ИмяПоля, ".Parent", ".Родитель");
	ИмяПоля = СтрЗаменить(ИмяПоля, ".Predefined", ".Предопределенный");
	ИмяПоля = СтрЗаменить(ИмяПоля, ".IsFolder", ".ЭтоГруппа");
	ИмяПоля = СтрЗаменить(ИмяПоля, ".Description", ".Наименование");
	Возврат ИмяПоля;
	
КонецФункции // ПолучитьИмяПоляВШаблоне()

Функция ПолучитьБлокируемыеРеквизитыОбъекта() Экспорт
	
	Результат = Новый Массив;
	Результат.Добавить("ТипШаблона");
	Результат.Добавить("ОбъектМетаданных");
	
	Возврат Результат;
	
КонецФункции // ПолучитьБлокируемыеРеквизитыОбъекта()

// Процедура получает массивы реквизитов, зависимых от реквизита КассаУправляющейСистемы.
// Параметры:
//           Объект - СправочникСсылка.Кассы - Элемент правочника для которого устанавливаем доступные реквизиты.
//           МассивВсехРеквизитов - Неопределено - Выходной параметр с типом Массив в который 
//           Будут помещены имена всех реквизитов справочника
//           МассивРеквизитовОперации - Неопределено - Выходной параметр с типом Массив в который
//           Будут помещены имена реквизитов справочника
//
Процедура ПолучитьМассивыРеквизитов(Объект, МассивВсехРеквизитов, МассивРеквизитов) Экспорт
	
	МассивВсехРеквизитов = Новый Массив;
	МассивВсехРеквизитов.Добавить("Наименование");
	МассивВсехРеквизитов.Добавить("ТипШаблона");
	МассивВсехРеквизитов.Добавить("ОбъектМетаданных");
	
	МассивРеквизитов = Новый Массив;
	МассивРеквизитов.Добавить("Наименование");
	МассивРеквизитов.Добавить("ТипШаблона");
	
	Если Объект.ТипШаблона = Перечисления.ТипыШаблонов.ФискальныйЧек
		ИЛИ Объект.ТипШаблона = Перечисления.ТипыШаблонов.ПустаяСсылка()
	Тогда
		МассивРеквизитов.Добавить("ОбъектМетаданных");
	КонецЕсли;
	
КонецПроцедуры // ПолучитьМассивыРеквизитов()


#Область ПроцедурыУправленияШаблонами

// Функция генерирует уникальный идентификатор строки дерева шаблона
//
// Параметры
//  Нет
//
// Возвращаемое значение:
//   <Строка>   - Уникальный идентификатор
//
Функция ПолучитьИдентификаторСтрокиШаблона() 
	
	Возврат "ID"+СтрЗаменить(Новый УникальныйИдентификатор,"-","_");
	
КонецФункции // ПолучитьИдентификатор()

////////// РАБОТА С СКД

// Для объекта получить макет СКД для подстановки в шаблоне
//
Функция ПолучитьСхемуСКДДляПодстановкиВШаблоне(МетаданныеОбъекта, ИмяМакета = "") Экспорт

	Имя = МетаданныеОбъекта.Имя;
	Если ПустаяСтрока(ИмяМакета) Тогда
		ИмяМакета = "ПоляШаблона";
	КонецЕсли;
	
	Если Метаданные.Справочники.Содержит(МетаданныеОбъекта) Тогда
		Возврат Справочники[Имя].ПолучитьМакет(ИмяМакета);
		
	ИначеЕсли Метаданные.Документы.Содержит(МетаданныеОбъекта) Тогда
		Возврат Документы[Имя].ПолучитьМакет(ИмяМакета);
		
	ИначеЕсли Метаданные.Отчеты.Содержит(МетаданныеОбъекта) Тогда
		Возврат Отчеты[Имя].ПолучитьМакет(ИмяМакета);
		
	ИначеЕсли Метаданные.Обработки.Содержит(МетаданныеОбъекта) Тогда
		Возврат Обработки[Имя].ПолучитьМакет(ИмяМакета);
		
	Иначе
		Возврат Неопределено;
		
	КонецЕсли;

КонецФункции // ПолучитьСхемуСКДДляПодстановкиВШаблоне()

// Среди элементов полей СКД найти поле по имени
//
Функция НайтиПолеСКДПоИмени(Элементы, Имя, Кэш = Неопределено)

	Если Кэш <> Неопределено Тогда
		Элемент = Кэш.Получить(Имя);
		Если Элемент <> Неопределено Тогда
			Возврат Элемент;
		КонецЕсли;
	КонецЕсли;
	
	Для Каждого Элемент Из Элементы Цикл
		Если ВРЕГ(Строка(Элемент.Поле)) = ВРЕГ(Имя) Тогда
			
			Если Кэш <> Неопределено Тогда
				Кэш.Вставить(Имя, Элемент);
			КонецЕсли;
			
			Возврат Элемент;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;

КонецФункции // НайтиПолеСКДПоИмени()

// Найти поле СКД по полному имени
//
Функция НайтиПолеСКДПоПолномуИмени(Элементы, ПолноеИмя, Кэш = Неопределено) Экспорт

	Если Кэш <> Неопределено Тогда
		Поле = Кэш.Получить(ПолноеИмя);
		Если Поле <> Неопределено Тогда
			Возврат Поле;
		КонецЕсли;
	КонецЕсли;
	
	масЧастейИмен = ИзПолногоИмениПоляПолучитьЧасти(ПолноеИмя);
	колЧастей = масЧастейИмен.Количество();
	
	текИмя = масЧастейИмен[0];
	Поле = НайтиПолеСКДПоИмени(Элементы, текИмя, Кэш);
	Если Поле = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Для Сч = 2 По колЧастей Цикл
		текИмя = текИмя +"." + масЧастейИмен[Сч-1];
		Поле = НайтиПолеСКДПоИмени(Поле.Элементы, текИмя, Кэш);
		Если Поле = Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЦикла;
	
	Если Кэш <> Неопределено Тогда
		Кэш.Вставить(ПолноеИмя, Поле);
	КонецЕсли;
	
	Возврат Поле;

КонецФункции // НайтиПолеСКДПоПолномуИмени()

#КонецОбласти

#Область ПроцедурыРаботыСФискальнымиЧеками

// Функция выполняет печать нефискальных строк на ФР
//
// Параметры
//  СтруктрураШаблона  - <Структура>, параметры шаблона
//  Свойство           - <Строка>, Наименование свойства
//
// Возвращаемое значение:
//   <Ошибка>   - Представление ошибки печати на ФР
//
Функция НапечататьНеФискальныеСтроки(СтруктрураШаблона, Свойство)Экспорт
	
	МассивСоответствий = Неопределено;
	СтруктрураШаблона.Шаблон.Свойство(Свойство,МассивСоответствий);
	Если МассивСоответствий <> Неопределено И МассивСоответствий.Количество() > 0 Тогда
		МассивСоответствий = ПреобразоватьМассивСоответствийВМассивТекстовыхСтрок(МассивСоответствий);
	КонецЕсли;
	
	Возврат ?(МассивСоответствий = Неопределено, Новый Массив(), МассивСоответствий);
	
КонецФункции // НапечататьНеФискальныеСтроки()

Функция НапечататьСтроки(СтруктрураШаблона, Свойство)Экспорт 
	
	МассивСтрок = Новый Массив();
	
	Если СтруктрураШаблона <> Неопределено Тогда
		МассивСтрок = НапечататьНеФискальныеСтроки(СтруктрураШаблона, Свойство);
	КонецЕсли;
	
	Возврат МассивСтрок;
	
КонецФункции // НапечататьСтроки()

// Функция преобразует массивы (из массива соответствий в массив текстовых строк)
//
// Параметры
//  МассивСоответствий  - Массив - Массив соответствий
//
// Возвращаемое значение:
//   <Массив>   - Массив текстовых строк
//
Функция ПреобразоватьМассивСоответствийВМассивТекстовыхСтрок(МассивСоответствий)Экспорт 

	РезультирующийМассив = Новый Массив;
	Для каждого Соответствия Из МассивСоответствий Цикл
		Для каждого ЭлементСоответствия Из Соответствия Цикл
			Для каждого СтрокаМассива Из ЭлементСоответствия.Значение Цикл
				РезультирующийМассив.Добавить(СтрокаМассива);
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
	
	Возврат РезультирующийМассив;
	
КонецФункции // ПреобразоватьМассивСоответствийВМассивТекстовыхСтрок()

// Функция выполняет добавление фискальных строк в дерево шаблона перед формированием
//  по нему представления чека
//
// Параметры
//  ИмяОбъекта            - Строка
//                          Имя объекта для которого нужно сформировать фискальные строки
//  КопияШаблона          - КоллекцияСтрокДереваЗначений
//                          Коллекция строк в котрую необходимо добавить фискальные строки
//  ШиринаЧека            - Число
//                          Ширина чека в симолах
//  ОднаФискальнаяСтрока  - Булево
//                          Определяется наличие режима одна фискальная строка
//
// Возвращаемое значение:
//   КоллекцияСтрокДереваЗначений   - Коллекция строк дерева значений
//
Функция СформироватьФискальныеСтрокиМакетаФискальногоЧека(ИмяОбъекта ,КопияШаблона, ШиринаЧека, ОднаФискальнаяСтрока)Экспорт 
	
	МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(ИмяОбъекта);
	Если МенеджерОбъекта = Неопределено Тогда
		Возврат КопияШаблона;
	Иначе
		Возврат МенеджерОбъекта.СформироватьФискальныеСтроки(КопияШаблона, ШиринаЧека, ОднаФискальнаяСтрока);
	КонецЕсли;
	
КонецФункции // СформироватьФискальныеСтрокиМакетаФискальногоЧека()

// Функция выполняет вставку строки в дерево шаблона по индексу с заданными параметрами
//
// Параметры
//  КоллекцияСтрок   - КоллекцияСтрокДереваЗначений
//                     Коллекция строк в котрую необходимо добавить новую строку
//  Индекс           - Число
//                     Индекс в коллекции значений
//  Параметры        - Структура:
//
//     ИмяЭлемента      - Строка
//                        Имя элемента
//     ТипЭлемента-     - Строка
//                        Тип элемента
//     Ширина           - Число
//                        Ширина в символах
//     РазмещениеТекста - Число
//                        Размещение текста
//     Выравнивание     - Строка
//                        Выравнивание текста
//     Формат           - Строка
//                        Формат строки
//     ИмяКолонки       - Строка
//                        Имя колонки для СКД
//     Вычислять        - Булево
//                        Необходимо ли вычислять значение элемента
//
// Возвращаемое значение:
//   СтрокаДереваЗначений   - Строка дерева значений
//
Функция ВставитьСтрокуВКоллекциюСтрокДерева(КоллекцияСтрок, Индекс, Параметры)Экспорт 

	НоваяСтрока = КоллекцияСтрок.Вставить(Индекс);
	
	Значение = Неопределено;
	Если Параметры.Свойство("Ширина", Значение) Тогда
		НоваяСтрока.Ширина        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Ширина        = 0;
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ИмяЭлемента", Значение) Тогда
		НоваяСтрока.Элемент        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Элемент        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ТипЭлемента", Значение) Тогда
		НоваяСтрока.ТипЭлемента        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ТипЭлемента        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Идентификатор", Значение) Тогда
		НоваяСтрока.Идентификатор        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Идентификатор        = ПолучитьИдентификаторСтрокиШаблона();
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("РазмещениеТекста", Значение) Тогда
		НоваяСтрока.РазмещениеТекста        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.РазмещениеТекста        = 1;
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Выравнивание", Значение) Тогда
		НоваяСтрока.Выравнивание        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Выравнивание        = "Лево";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ИмяКолонки", Значение) Тогда
		НоваяСтрока.ИмяКолонки        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ИмяКолонки        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Формат", Значение) Тогда
		НоваяСтрока.Формат        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Формат        = "";
	КонецЕсли;

	Значение = Неопределено;
	Если Параметры.Свойство("Префикс", Значение) Тогда
		НоваяСтрока.Префикс        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Префикс        = "";
	КонецЕсли;

	Значение = Неопределено;
	Если Параметры.Свойство("Постфикс", Значение) Тогда
		НоваяСтрока.Постфикс        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Постфикс        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ОписаниеТипа", Значение) Тогда
		НоваяСтрока.ОписаниеТипа        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ОписаниеТипа = Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки(50));
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ПустоеЗначение", Значение) Тогда
		НоваяСтрока.ПустоеЗначение        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ПустоеЗначение = Неопределено;
	КонецЕсли;

	Значение = Неопределено;
	Если Параметры.Свойство("СтрокаПустоеЗначение", Значение) Тогда
		НоваяСтрока.СтрокаПустоеЗначение        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.СтрокаПустоеЗначение = "";
	КонецЕсли;
	
	Возврат НоваяСтрока;
	
КонецФункции // ДобавитьСтрокуВКоллекциюСтрокДерева()

// функция выполняет добавление строки в дерево шаблона с заданными параметрами
//
// Параметры
//  КоллекцияСтрок   - КоллекцияСтрокДереваЗначений
//                     Коллекция строк в котрую необходимо добавить новую строку
//
//  Параметры        - Структура:
//
//     ИмяЭлемента      - Строка
//                        Имя элемента
//     ТипЭлемента-     - Строка
//                        Тип элемента
//     Ширина           - Число
//                        Ширина в символах
//     РазмещениеТекста - Число
//                        Размещение текста
//     Выравнивание     - Строка
//                        Выравнивание текста
//     Формат           - Строка
//                        Формат строки
//     ИмяКолонки       - Строка
//                        Имя колонки для СКД
//     Вычислять        - Булево
//                        Необходимо ли вычислять значение элемента
//
// Возвращаемое значение:
//   СтрокаДереваЗначений   - Строка дерева значений
//
Функция ДобавитьСтрокуВКоллекциюСтрокДерева(КоллекцияСтрок, Параметры)Экспорт 

	НоваяСтрока = КоллекцияСтрок.Добавить();
	
	Значение = Неопределено;
	Если Параметры.Свойство("Ширина", Значение) Тогда
		НоваяСтрока.Ширина        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Ширина        = 0;
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ИмяЭлемента", Значение) Тогда
		НоваяСтрока.Элемент        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Элемент        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ТипЭлемента", Значение) Тогда
		НоваяСтрока.ТипЭлемента        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ТипЭлемента        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Идентификатор", Значение) Тогда
		НоваяСтрока.Идентификатор        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Идентификатор        = ПолучитьИдентификаторСтрокиШаблона();
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("РазмещениеТекста", Значение) Тогда
		НоваяСтрока.РазмещениеТекста        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.РазмещениеТекста        = 1;
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Выравнивание", Значение) Тогда
		НоваяСтрока.Выравнивание        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Выравнивание        = "Лево";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ИмяКолонки", Значение) Тогда
		НоваяСтрока.ИмяКолонки        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ИмяКолонки        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Формат", Значение) Тогда
		НоваяСтрока.Формат        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Формат        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Вычислять", Значение) Тогда
		НоваяСтрока.Вычислять        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Вычислять        = Ложь;
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Префикс", Значение) Тогда
		НоваяСтрока.Префикс        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Префикс        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("Постфикс", Значение) Тогда
		НоваяСтрока.Постфикс        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.Постфикс        = "";
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ОписаниеТипа", Значение) Тогда
		НоваяСтрока.ОписаниеТипа        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ОписаниеТипа = Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки(50));
	КонецЕсли;
	
	Значение = Неопределено;
	Если Параметры.Свойство("ПустоеЗначение", Значение) Тогда
		НоваяСтрока.ПустоеЗначение        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.ПустоеЗначение = Неопределено;
	КонецЕсли;

	Значение = Неопределено;
	Если Параметры.Свойство("СтрокаПустоеЗначение", Значение) Тогда
		НоваяСтрока.СтрокаПустоеЗначение        = Значение;
	Иначе
		// Значение по умолчанию
		НоваяСтрока.СтрокаПустоеЗначение = "";
	КонецЕсли;
	
	Если НоваяСтрока.ТипЭлемента = "ОбластьЧека" Тогда
		НоваяСтрока.ИндексКартинки = 0.00; // СистемнаяСтрока
	ИначеЕсли НоваяСтрока.ТипЭлемента = "СтрокаДанных" Тогда
		НоваяСтрока.ИндексКартинки = 1.00; // ВычисляемаяСтрока
	ИначеЕсли НоваяСтрока.ТипЭлемента = "СтрокаТекста" Тогда
		НоваяСтрока.ИндексКартинки = 2.00; // ПользовательскаяСтрока
	ИначеЕсли НоваяСтрока.ТипЭлемента = "СоставнаяСтрока" Тогда
		НоваяСтрока.ИндексКартинки = 3.00; // СоставнаяСтрока
	ИначеЕсли НоваяСтрока.ТипЭлемента = "Таблица" Тогда
		НоваяСтрока.ИндексКартинки = 4.00; // СтрокаТаблицы
	Иначе
		НоваяСтрока.ИндексКартинки = 5.00;
	КонецЕсли;
	
	Возврат НоваяСтрока;
	
КонецФункции // ДобавитьСтрокуВКоллекциюСтрокДерева()
         
// Процедура выполняет формирование предопределенной структуры шаблона
//  значений
//
// Параметры
//  ИерархическийШаблон -  ДеревоЗначений - Шаблон для которого необходмо сформировать предопредленную структуру
//
Функция СформироватьПервичнуюСтруктуруИерархическогоШаблона(ИерархическийШаблон, ИмяОбъекта, ПервичнаяСтруктура, Загружать = Истина)Экспорт 
	
	МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(ИмяОбъекта);
	Возврат МенеджерОбъекта.СформироватьПервичнуюСтруктуруШаблона(ИерархическийШаблон, ИмяОбъекта, ПервичнаяСтруктура, Загружать);
	
КонецФункции // СформироватьПервичнуюСтруктуруИерархическогоШаблона()

// Процедура заполняет массив непроверяемых реквизитов значениями,
// Не используемыми при выбранной хозяйственной операции документа.
//
// Параметры:
//	МассивВсехРеквизитов - Массив - Массив всех реквизитов документа
//	МассивРеквизитовОперации - Массив - Массив реквизитов документа, которые относятся к хозяйственной операции документа
//	МассивНепроверяемыхРеквизитов - Массив реквизитов объекта, не требующих проверки
//
Процедура ЗаполнитьМассивНепроверяемыхРеквизитов(МассивВсехРеквизитов, МассивРеквизитовОперации, МассивНепроверяемыхРеквизитов)Экспорт 
	
	Для Каждого ЭлементМассива Из МассивВсехРеквизитов Цикл
		
		// Если реквизит не используется для хозяйственной операции, исключаем данный реквизит из проверки.
		Если МассивРеквизитовОперации.Найти(ЭлементМассива) = Неопределено Тогда
			МассивНепроверяемыхРеквизитов.Добавить(ЭлементМассива);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры // ЗаполнитьМассивНепроверяемыхРеквизитов()

#КонецОбласти

#Область ВспомогательныеФункции

// Полное имя поля разделить по частям
//
Функция ИзПолногоИмениПоляПолучитьЧасти(ПолноеИмя)

	масЧастей = Новый Массив;
	СтрИмя = ПолноеИмя;
	
	Пока Не ПустаяСтрока(СтрИмя) Цикл
		Если Лев(СтрИмя, 1) = "[" Тогда
			
			Поз = Найти(СтрИмя, "]");
			Если Поз = 0 Тогда
				масЧастей.Добавить(Сред(СтрИмя, 2));
				СтрИмя = "";
			Иначе
				масЧастей.Добавить(Сред(СтрИмя, 1, Поз));
				СтрИмя = Сред(СтрИмя, Поз + 2);
			КонецЕсли;
			
		Иначе
			
			Поз = Найти(СтрИмя, ".");
			Если Поз = 0 Тогда
				масЧастей.Добавить(СтрИмя);
				СтрИмя = "";
			Иначе
				масЧастей.Добавить(Лев(СтрИмя, Поз - 1));
				СтрИмя = Сред(СтрИмя, Поз + 1);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат масЧастей;

КонецФункции // ИзПолногоИмениПоляПолучитьЧасти()

// Получить все шаблоны для пластиковых карт
Функция ПолучитьШаблоныПластиковыхКарт() Экспорт
	
	Результат = Новый Массив;
	
	ЗапросКарты = Новый Запрос(
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ХранилищеШаблонов.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ХранилищеШаблонов КАК ХранилищеШаблонов
	|ГДЕ
	|	ХранилищеШаблонов.ТипШаблона = ЗНАЧЕНИЕ(Перечисление.ТипыШаблонов.ПластиковаяКарта)
	|	И НЕ ХранилищеШаблонов.ПометкаУдаления");	
	
	Выборка = ЗапросКарты.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Добавить(Выборка.ссылка);
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

// Процедура заполяет шаблоны в табличном документе по переданным параметрам
Процедура ЗаполнитьШаблоныВТабличномДокументе(ТабДокумент,ПараметрыЗаполнения) Экспорт
	
	Для Каждого Параметр ИЗ ПараметрыЗаполнения Цикл
		
		ИмяПараметра      = Параметр.ИмяПараметра;
		ЗначениеПараметра = Параметр.ЗначениеПараметра;
		
		Рисунок = НайтиРисунокТабДокументаПоИмени(ТабДокумент,ИмяПараметра);
		
		// Если не удалось найти рисунок по наименованию
		Если Рисунок = Неопределено Тогда			
			Продолжить;
		КонецЕсли;	
		
		Если ТипЗнч(ЗначениеПараметра) = Тип("Картинка") Тогда
			
			Рисунок.Картинка = ЗначениеПараметра;
			
		ИначеЕсли ТипЗнч(ЗначениеПараметра) = Тип("Строка") Тогда
			
			Рисунок.Текст = ЗначениеПараметра;
			
		КонецЕсли;
		
	КонецЦикла;
			
КонецПроцедуры

// Функция поиска рисунка в ТД по наименованию
Функция НайтиРисунокТабДокументаПоИмени(ТабДокумент,Имя)
		
	Для Каждого Рисунок ИЗ ТабДокумент.Рисунки Цикл
		
		Если Рисунок.Имя = Имя Тогда
			Возврат Рисунок;
		КонецЕсли;	
				
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции 

#КонецОбласти

#КонецОбласти