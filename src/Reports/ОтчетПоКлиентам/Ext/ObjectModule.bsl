
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

// Процедура - При компоновке результата
//
// Параметры:
//  ДокументРезультат	 - 	 - 
//  ДанныеРасшифровки	 - 	 - 
//  СтандартнаяОбработка - 	 - 
//
Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	ОтчетыСервер.ПриКомпоновкеРезультата(КомпоновщикНастроек, СхемаКомпоновкиДанных, ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка);
	//Если ПараметрыСеанса.ТекущийПользователь.РольПользователя <> Перечисления.РольПользователя.Руководитель И
	//	ПараметрыСеанса.ТекущийПользователь.РольПользователя <> Перечисления.РольПользователя.Управляющий Тогда
	//	
	//	ИД = ЭтотОбъект.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы.Найти("Период").ИдентификаторПользовательскойНастройки;
	//	Если ИД = Неопределено ТОгда 
	//		возврат;
	//	КонецЕсли;
	//	
	//	ПараметрПериод = ЭтотОбъект.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы.Найти(ИД);
	//	
	//	Разрешено = (ПараметрПериод.Использование) И
	//	((ПараметрПериод.Значение.Вариант = ВариантСтандартногоПериода.Сегодня) ИЛИ
	//	(ПараметрПериод.Значение.Вариант = ВариантСтандартногоПериода.Вчера) ИЛИ
	//	(ПараметрПериод.Значение.ДатаНачала > НачалоДня(ТекущаяДата()-24*60*60)));
	//	Если НЕ Разрешено Тогда
	//		ПараметрПериод.Значение.Вариант = ВариантСтандартногоПериода.Сегодня;
	//		ПараметрПериод.Значение.ДатаНачала = НачалоДня(ТекущаяДата());
	//		ПараметрПериод.Значение.ДатаОкончания = КонецДня(ТекущаяДата());
	//		ПараметрПериод.Использование = Истина;
	//	КонецЕсли;
	//КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Процедура - При определении настроек отчета
//
// Параметры:
//  НастройкиОтчета		 - 	 - 
//  НастройкиВариантов	 - 	 - 
//
Процедура ПриОпределенииНастроекОтчета(НастройкиОтчета, НастройкиВариантов) Экспорт
	
	НастройкиВариантов["ОтчетПоКлиентам"].Рекомендуемый = Истина;
//	НастройкиВариантов["ДниРождения"].Рекомендуемый     = Ложь;
	НастройкиВариантов["НовыеКлиенты"].Рекомендуемый    = Ложь;

	ЗаполнитьПредопределенныеВариантыОформления(НастройкиВариантов);
	
КонецПроцедуры

// Процедура - Заполнить предопределенные варианты оформления
//
// Параметры:
//  НастройкиВариантов	 - 	 - 
//
Процедура ЗаполнитьПредопределенныеВариантыОформления(НастройкиВариантов)
	
	МассивПолейСумм = Новый Массив;
	
	Для Каждого ДоступноеПоле Из КомпоновщикНастроек.Настройки.ДоступныеПоляВыбора.Элементы Цикл
		
		Если Не ДоступноеПоле.Ресурс Тогда
			Продолжить;
		КонецЕсли;
		
		ИмяПоля = Строка(ДоступноеПоле.Поле);
		МассивПолейСумм.Добавить(ИмяПоля);
		
	КонецЦикла; 
	
	Для Каждого НастройкиТекВарианта Из НастройкиВариантов Цикл
		
		ВариантыОформления = НастройкиТекВарианта.Значение.ВариантыОформления;
		ОтчетыСервер.ДобавитьВариантыОформленияСумм(ВариантыОформления, МассивПолейСумм);
			
	КонецЦикла; 
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли

	


