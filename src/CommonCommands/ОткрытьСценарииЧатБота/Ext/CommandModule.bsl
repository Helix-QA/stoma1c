
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	//ПараметрыФормы = Новый Структура("", );
	Если ИспользуетсяРасширениеИнтеграцияЧатБотов() Тогда 
		ОткрытьФорму("Справочник.СценарииЧатБотов.ФормаСписка", , ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно, ПараметрыВыполненияКоманды.НавигационнаяСсылка);
	Иначе 
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Расширение для работы с чат-ботом не подключено или не активно!'; en = 'The extension for working with the chatbot is not connected or is not active!'"));
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
// Функция - Используется расширение интеграция чат ботов
// 
// Возвращаемое значение:
//   - 
//
Функция ИспользуетсяРасширениеИнтеграцияЧатБотов()
	
	Возврат ОбщегоНазначенияСтоматология.ИспользуетсяРасширение("Мессенджер");
	
КонецФункции	

#КонецОбласти