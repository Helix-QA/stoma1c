

#Область СлужебныйПрограммныйИнтерфейс

#Область ОбработчикиСобытийРеквизитов

// Процедура - обработчик события "ПриИзменении" для поля ввода тега в форме объекта
//
// Параметры:
//  Форма	 - форма объекта
//  Элемент	 - поле ввода тега
Процедура ТегПриИзменении(Форма, Элемент) Экспорт
	
	ПоместитьТегВТабличнуюЧасть(Форма);
	
КонецПроцедуры

// Процедура - обработчик события "ОбработкаНавигационнойСсылки" для поля значения тега в форме объекта
//
// Параметры:
//  Форма				 - форма объекта
//  Элемент				 - элемент формы - значение тега
//  НавигационнаяСсылка	 - строка - навигационная ссылка содержащая ИД тега
//  СтандартнаяОбработка - булево - стандартная обработка навигационной ссылки
Процедура ТегОбработкаНавигационнойСсылки(Форма, Элемент, НавигационнаяСсылка, СтандартнаяОбработка) Экспорт
	
	Если Лев(НавигационнаяСсылка, 6) <> "ТегИД_" Тогда
		Возврат;
	КонецЕсли;
	
	Объект = Форма.Объект;
	
	СтандартнаяОбработка = Ложь;
	Форма.Модифицированность = Истина;
	
	Форма.ЗаблокироватьДанныеФормыДляРедактирования();
	
	ТегИД = Сред(НавигационнаяСсылка, 7);
	ЭлементСписка = Объект.Теги.НайтиПоИдентификатору(ТегИД);
	
	Объект.Теги.Удалить(ЭлементСписка);
	
	Форма.ОбновитьОблакоТегов();
	
КонецПроцедуры

// Процедура - обработчик события "ПриИзменении" для поля ввода тега в форме объекта
//
// Параметры:
//  Форма	 - форма объекта
//  Элемент	 - поле ввода тега
Процедура ИнтересПриИзменении(Форма, Элемент) Экспорт
	
	ПоместитьИнтересВТабличнуюЧасть(Форма);
	
КонецПроцедуры

// Процедура - обработчик события "ОбработкаНавигационнойСсылки" для поля значения тега в форме объекта
//
// Параметры:
//  Форма				 - форма объекта
//  Элемент				 - элемент формы - значение тега
//  НавигационнаяСсылка	 - строка - навигационная ссылка содержащая ИД тега
//  СтандартнаяОбработка - булево - стандартная обработка навигационной ссылки
Процедура ИнтересОбработкаНавигационнойСсылки(Форма, Элемент, НавигационнаяСсылка, СтандартнаяОбработка) Экспорт
	
	Если Лев(НавигационнаяСсылка, 10) <> "ИнтересИД_" Тогда
		Возврат;
	КонецЕсли;
	
	Объект = Форма.Объект;
	
	СтандартнаяОбработка = Ложь;
	Форма.Модифицированность = Истина;
	
	Форма.ЗаблокироватьДанныеФормыДляРедактирования();
	
	ИнтересИД = Сред(НавигационнаяСсылка, 11);
	ЭлементСписка = Объект.Интересы.НайтиПоИдентификатору(ИнтересИД);
	
	Объект.Интересы.Удалить(ЭлементСписка);
	
	Форма.ОбновитьОблакоИнтересов();
	
КонецПроцедуры

// Процедура - обработчик события "ОбработкаНавигационнойСсылки" для поля значения тега в форме объекта
//
// Параметры:
//  Форма				 - форма объекта
//  Элемент				 - элемент формы - значение тега
//  НавигационнаяСсылка	 - строка - навигационная ссылка содержащая ИД тега
//  СтандартнаяОбработка - булево - стандартная обработка навигационной ссылки
Процедура ЗаметкаОбработкаНавигационнойСсылки(Форма, Элемент, НавигационнаяСсылка, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	Объект = Форма.Объект;

	ЗаметкаИД	  = Сред(НавигационнаяСсылка, 25);
	ЭлементСписка = Объект.Заметки.Получить(ЗаметкаИД);         			
	Обработчик	  = Новый ОписаниеОповещения("ЗаметкаОбработкаНавигационнойСсылкиЗавершение", ЭтотОбъект, Новый Структура("ЭлементСписка, Форма", ЭлементСписка, Форма));
	// ОткрытьФорму("Справочник.Контрагенты.Форма.ФормаРедактированияЗаметки", Новый Структура("ТекстЗаметки", ЭлементСписка.Заметка),,,,, Обработчик, РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
	
КонецПроцедуры

// Процедура завершает редактирование заметки
//
Процедура ЗаметкаОбработкаНавигационнойСсылкиЗавершение(Результат, Параметры) Экспорт 
	
	Если Результат <> Неопределено Тогда 
		Параметры.ЭлементСписка.Заметка = Результат;
		Форма = Параметры.Форма;
		Форма.Модифицированность = Истина;
		Форма.ОбновитьОблакоЗаметок();  		
	КонецЕсли;
	
КонецПроцедуры

// Функция - обработчик события "Нажатие" для предопределенных вариантов отбора по статусу клиента
//
// Параметры:
//  Форма			 - форма списка
//  ИмяСписка		 - строка - имя динамического списка формы для которого устанавливается отбор
//  Статус	 		 - ПеречислениеСсылка - ссылка на выбранное перечисление
//  Элемент			 - элемент формы
// Возвращаемое значение:
//  Булево - включен или выключен отбор по данному элементу
Функция СтатусОтборНажатие(Форма, ИмяСписка, ПредставлениеНовогоСтатуса, Элемент) Экспорт
	
	СоответствиеПредставленийСтатусов = КлассификацияКонтактовВызовСервера.ПолучитьСоответствиеПредставленийСтатусовКлиентов();
	НовыйСтатус = СоответствиеПредставленийСтатусов[ПредставлениеНовогоСтатуса];
	ОтборВключен = Ложь;
	
	Для каждого СтрокаСоответствия Из СоответствиеПредставленийСтатусов Цикл
		
		ЭлементОтображенияСтатуса = Форма.Элементы["ОтборСтатус" + СтрокаСоответствия.Ключ];
		
		Если СтрокаСоответствия.Ключ = ПредставлениеНовогоСтатуса Тогда
			Если Форма.СтатусОтбор = НовыйСтатус Тогда // нажали на уже активный отбор - отбор нужно сбросить
				ОтборВключен = Ложь;
				ЭлементОтображенияСтатуса.ЦветФона = Новый Цвет;
			Иначе // нажали на не активный доселе отбор - обновляем отбор
				ОтборВключен = Истина;
				ЭлементОтображенияСтатуса.ЦветФона = ОбщегоНазначенияКлиентПовтИсп.ЦветСтиля("ФонАктивногоЗначенияОтбора");
			КонецЕсли;
		Иначе
			ЭлементОтображенияСтатуса.ЦветФона = Новый Цвет;
		КонецЕсли; 
		
	КонецЦикла;
	
	Форма.СтатусОтбор = ?(ОтборВключен, НовыйСтатус, Неопределено);
	
	СформироватьЗаголовокВариантаОтбора(Форма.Элементы.ОтборСтатус, ?(ОтборВключен, 1, 0));
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Форма[ИмяСписка], "Статус", НовыйСтатус, ВидСравненияКомпоновкиДанных.Равно, , ОтборВключен);
	
	Возврат ОтборВключен;
	
КонецФункции

// Функция - обработчик события "Нажатие" для предопределенных вариантов периодов 
// Или события "ПриИзменении" для произвольного периода
//
// Параметры:
//  Форма			 - форма списка
//  ИмяСписка		 - строка - имя динамического списка формы для которого устанавливается отбор
//  ВариантПериода	 - строка - принимает значения: "ПроизвольныйПериод", "Сегодня", "3дня", "Неделя", "Месяц"
//  Элемент			 - элемент формы
// Возвращаемое значение:
//  Булево - включен или выключен отбор по данному элементу
Функция СозданыОтборНажатие(Форма, ИмяСписка, ВариантПериода, Элемент) Экспорт
	
	Если ВариантПериода = "ПроизвольныйПериод" Тогда
		НомерПериода = 0;
	ИначеЕсли ВариантПериода = "Сегодня" Тогда
		НомерПериода = 1;
	ИначеЕсли ВариантПериода = "3Дня" Тогда
		НомерПериода = 2;
	ИначеЕсли ВариантПериода = "Неделя" Тогда
		НомерПериода = 3;
	ИначеЕсли ВариантПериода = "Месяц" Тогда
		НомерПериода = 4;
	КонецЕсли;
	
	Период = Форма.ОтборСозданы[НомерПериода];
	Если НомерПериода = 0 Тогда
		Если ЗначениеЗаполнено(Период.Значение.ДатаНачала) Или ЗначениеЗаполнено(Период.Значение.ДатаОкончания) Тогда
			Период.Пометка = Истина;
		Иначе
			Период.Пометка = Ложь;
		КонецЕсли;
	Иначе
		Период.Пометка = Не Период.Пометка;
	КонецЕсли;
	
	// Вариант периода может быть выбран только один
	Для Индекс = 0 По Форма.ОтборСозданы.Количество()-1 Цикл
		
		ЭлементСпискаПериодов = Форма.ОтборСозданы[Индекс];
		Если ЭлементСпискаПериодов <> Период Тогда
			ЭлементСпискаПериодов.Пометка = Ложь;
		КонецЕсли;
		
		Если Индекс = 0 Тогда
			ЭлементОтображенияПериода = Форма.Элементы.ОтборСозданыПроизвольныйПериод;
		ИначеЕсли Индекс = 1 Тогда
			ЭлементОтображенияПериода = Форма.Элементы.ОтборСозданыСегодня;
		ИначеЕсли Индекс = 2 Тогда
			ЭлементОтображенияПериода = Форма.Элементы.ОтборСозданыЗа3Дня;
		ИначеЕсли Индекс = 3 Тогда
			ЭлементОтображенияПериода = Форма.Элементы.ОтборСозданыЗаНеделю;
		ИначеЕсли Индекс = 4 Тогда
			ЭлементОтображенияПериода = Форма.Элементы.ОтборСозданыЗаМесяц;
		Иначе
			Продолжить;
		КонецЕсли;
		
		Если ЭлементСпискаПериодов.Пометка Тогда
			ЭлементОтображенияПериода.ЦветФона = ОбщегоНазначенияКлиентПовтИсп.ЦветСтиля("ФонАктивногоЗначенияОтбора");
		Иначе
			ЭлементОтображенияПериода.ЦветФона = Новый Цвет;
		КонецЕсли;
		
	КонецЦикла;
	
	СформироватьЗаголовокВариантаОтбора(Форма.Элементы.ОтборПериод, ОбщегоНазначенияКлиентСервер.ПолучитьМассивОтмеченныхЭлементовСписка(Форма.ОтборСозданы).Количество());
	
	ГруппаОтбораПериод = ОбщегоНазначенияКлиентСервер.СоздатьГруппуЭлементовОтбора(Форма[ИмяСписка].Отбор.Элементы, "ОтборПериод", ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ);
	ГруппаОтбораПериод.Использование = Период.Пометка;
	Если ГруппаОтбораПериод.Элементы.Количество() = 0 Тогда
		
		ЭлементОтбора = ГруппаОтбораПериод.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ДатаРегистрации");
		ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.БольшеИлиРавно;
		ЭлементОтбора.ПравоеЗначение = Период.Значение.ДатаНачала;
		ЭлементОтбора.Использование = Истина;
		
		ЭлементОтбора = ГруппаОтбораПериод.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ДатаРегистрации");
		ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.МеньшеИлиРавно;
		ЭлементОтбора.ПравоеЗначение = Период.Значение.ДатаОкончания;
		ЭлементОтбора.Использование = Истина;
		
	ИначеЕсли ГруппаОтбораПериод.Элементы.Количество() = 2 Тогда
		
		ГруппаОтбораПериод.Элементы[0].ПравоеЗначение = Период.Значение.ДатаНачала;
		ГруппаОтбораПериод.Элементы[1].ПравоеЗначение = Период.Значение.ДатаОкончания;
		
	КонецЕсли;
	
	Возврат Период.Пометка;
	
КонецФункции

// Функция - обработчик события "Нажатие" для полей-тегов в форме списка
//
// Параметры:
//  Форма				 - форма списка
//  ИмяСписка			 - строка - имя динамического списка формы для которого устанавливается отбор
//  Элемент				 - элемент формы
//  СтандартнаяОбработка - булево - стандартная обработка нажатия
// Возвращаемое значение:
//  булево - включен или выключен отбор по данному элементу
Функция ТегОтборНажатие(Форма, ИмяСписка, Элемент, СтандартнаяОбработка) Экспорт
	
	Если Лев(Элемент.Имя, 4) <> "Тег_" Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СтандартнаяОбработка = Ложь;
	
	// Если Элемент.Имя = "Тег_Пояснение" Тогда
	//	ПараметрыОткрытия = Новый Структура("Заголовок, КлючПодсказки", 
	//		"Как работать с тегами",
	//		"КлассификацияКонтрагентов_КакРаботатьСТегами");
	//	ОткрытьФорму("Обработка.МенеджерПодсказок.Форма", ПараметрыОткрытия, , Форма.УникальныйИдентификатор);
	//	Возврат Неопределено;
	// КонецЕсли;
	
	ТегИД = Сред(Элемент.Имя, 5);
	ЭлементСписка = Форма.ОтборТеги.НайтиПоИдентификатору(ТегИД);
	ЭлементСписка.Пометка = НЕ ЭлементСписка.Пометка;
	
	ОтмеченныеТеги = ОбщегоНазначенияКлиентСервер.ПолучитьМассивОтмеченныхЭлементовСписка(Форма.ОтборТеги);
	ОтмеченныеСегменты = ОбщегоНазначенияКлиентСервер.ПолучитьМассивОтмеченныхЭлементовСписка(Форма.ОтборСегменты);
	СформироватьЗаголовокВариантаОтбора(Форма.Элементы.ОтборТеги, ОтмеченныеТеги.Количество());
	
	Контрагенты = Новый Массив;
	Если ОтмеченныеТеги.Количество() > 0 Или ОтмеченныеСегменты.Количество() > 0 Тогда
		Контрагенты = КлассификацияКонтактовВызовСервера.КонтрагентыПоТегамИСегментам(ОтмеченныеТеги, ОтмеченныеСегменты);
		ОтборВключен = Истина;
	Иначе
		Контрагенты = Новый Массив;
		ОтборВключен = Ложь;
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Форма[ИмяСписка], "Ссылка", Контрагенты, ВидСравненияКомпоновкиДанных.ВСписке, , ОтборВключен);
	
	Возврат ЭлементСписка.Пометка;
	
КонецФункции

// Функция - обработчик события "Нажатие" для полей-сегментов в форме списка
//
// Параметры:
//  Форма				 - форма списка
//  ИмяСписка			 - строка - имя динамического списка формы для которого устанавливается отбор
//  Элемент				 - элемент формы
//  СтандартнаяОбработка - булево - стандартная обработка нажатия
// Возвращаемое значение:
//  булево - включен или выключен отбор по данному элементу
Функция СегментОтборНажатие(Форма, ИмяСписка, Элемент, СтандартнаяОбработка) Экспорт
	
	Если Лев(Элемент.Имя, 8) <> "Сегмент_" Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СтандартнаяОбработка = Ложь;
	
	// Если Элемент.Имя = "Сегмент_Пояснение" Тогда
	//	ПараметрыОткрытия = Новый Структура("Заголовок, КлючПодсказки", 
	//		"Как работать с сегментами",
	//		"КлассификацияКонтрагентов_КакРаботатьССегментами");
	//	ОткрытьФорму("Обработка.МенеджерПодсказок.Форма", ПараметрыОткрытия, , Форма.УникальныйИдентификатор);
	//	Возврат Неопределено;
	// КонецЕсли;
	
	СегментИД = Сред(Элемент.Имя, 9);
	ЭлементСписка = Форма.ОтборСегменты.НайтиПоИдентификатору(СегментИД);
	ЭлементСписка.Пометка = НЕ ЭлементСписка.Пометка;
	
	Теги = ОбщегоНазначенияКлиентСервер.ПолучитьМассивОтмеченныхЭлементовСписка(Форма.ОтборТеги);
	ОтмеченныеСегменты = ОбщегоНазначенияКлиентСервер.ПолучитьМассивОтмеченныхЭлементовСписка(Форма.ОтборСегменты);
	СформироватьЗаголовокВариантаОтбора(Форма.Элементы.ОтборСегменты, ОтмеченныеСегменты.Количество());
	
	Контрагенты = Новый Массив;
	Если Теги.Количество() > 0 Или ОтмеченныеСегменты.Количество() > 0 Тогда
		Контрагенты = КлассификацияКонтактовВызовСервера.КонтрагентыПоТегамИСегментам(Теги, ОтмеченныеСегменты);
		ОтборВключен = Истина;
	Иначе
		Контрагенты = Новый Массив;
		ОтборВключен = Ложь;
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Форма[ИмяСписка], "Ссылка", Контрагенты, ВидСравненияКомпоновкиДанных.ВСписке, , ОтборВключен);
	
	Возврат ЭлементСписка.Пометка;
	
КонецФункции

Функция МенеджерОтборПриИзменении(Форма, ИмяСписка, Элемент) Экспорт
	
	ОтборВключен = ЗначениеЗаполнено(Форма.МенеджерОтбор);
	СформироватьЗаголовокВариантаОтбора(Форма.Элементы.ОтборМенеджер, ?(ОтборВключен, 1, 0));
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Форма[ИмяСписка], "Менеджер", Форма.МенеджерОтбор, ВидСравненияКомпоновкиДанных.Равно, , ОтборВключен);
		
	Возврат ОтборВключен; 
	
КонецФункции

Функция РекламныйИсточникОтборПриИзменении(Форма, ИмяСписка, Элемент) Экспорт
	
	ОтборВключен = ЗначениеЗаполнено(Форма.РекламныйИсточникОтбор);
	СформироватьЗаголовокВариантаОтбора(Форма.Элементы.ОтборРекламныйИсточник, ?(ОтборВключен, 1, 0));
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Форма[ИмяСписка], "ИсточникИнформации", Форма.РекламныйИсточникОтбор, ВидСравненияКомпоновкиДанных.Равно, , ОтборВключен);
		
	Возврат ОтборВключен; 
	
КонецФункции

#КонецОбласти

#Область ОбработчикиКоманд

// Процедура - обработчик смены варианта отбора на панели отборов
//
// Параметры:
//  Форма	 - форма списка
//  Команда	 - команда формы
Процедура ВыборВариантаОтбора(Форма, Команда) Экспорт
	
	Элементы = Форма.Элементы;
	
	Если Команда.Имя = "ОтборСтатус" Тогда
		ИмяСтраницы = "ЗначенияОтбораСтатус";
	ИначеЕсли Команда.Имя = "ОтборПериод" Тогда
		ИмяСтраницы = "ЗначенияОтбораПериод";
	ИначеЕсли Команда.Имя = "ОтборТеги" Тогда
		ИмяСтраницы = "ЗначенияОтбораТеги";
	ИначеЕсли Команда.Имя = "ОтборСегменты" Тогда
		ИмяСтраницы = "ЗначенияОтбораСегменты";
	ИначеЕсли Команда.Имя = "ОтборМенеджер" Тогда
		ИмяСтраницы = "ЗначенияОтбораМенеджер";
	ИначеЕсли Команда.Имя = "ОтборРекламныйИсточник" Тогда
		ИмяСтраницы = "ЗначенияОтбораРекламныйИсточник";
	Иначе
		Возврат;
	КонецЕсли;
	
	СтраницаКОтображению = Элементы.Найти(ИмяСтраницы);
	Если СтраницаКОтображению = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Элементы.ПанельЗначенийОтбора.Видимость И Элементы.ПанельЗначенийОтбора.ТекущаяСтраница = СтраницаКОтображению Тогда
		Элементы.ПанельЗначенийОтбора.Видимость = Ложь;
		Элементы[Команда.Имя].ЦветФона = Новый Цвет;
	Иначе
		Элементы.ПанельЗначенийОтбора.Видимость = Истина;
		Элементы.ПанельЗначенийОтбора.ТекущаяСтраница = СтраницаКОтображению;
		
		Элементы.ОтборПериод.ЦветФона = Новый Цвет;
		Элементы.ОтборТеги.ЦветФона = Новый Цвет;
		Элементы.ОтборСегменты.ЦветФона = Новый Цвет;
		
		Если Элементы.Найти("ОтборСтатус") <> Неопределено Тогда
			Элементы.ОтборСтатус.ЦветФона = Новый Цвет;
		    Элементы.ОтборМенеджер.ЦветФона = Новый Цвет;
			Элементы.ОтборРекламныйИсточник.ЦветФона = Новый Цвет;
		КонецЕсли;
		
		Элементы[Команда.Имя].ЦветФона = ОбщегоНазначенияКлиентПовтИсп.ЦветСтиля("ФонАктивногоЗначенияОтбора");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПоместитьТегВТабличнуюЧасть(Форма)
	
	Если НЕ ЗначениеЗаполнено(Форма.Тег) Тогда
		Возврат;
	КонецЕсли;
	
	Объект = Форма.Объект;
	
	Если Объект.Теги.НайтиСтроки(Новый Структура("Тег", Форма.Тег)).Количество() = 0 Тогда
		
		Форма.ЗаблокироватьДанныеФормыДляРедактирования();
		Форма.Модифицированность = Истина;
		
		НоваяСтрока = Объект.Теги.Добавить();
		НоваяСтрока.Тег = Форма.Тег;
		
		Форма.ОбновитьОблакоТегов();
		
	КонецЕсли;
	
	Форма.Тег = Неопределено;
	
КонецПроцедуры

Процедура ПоместитьИнтересВТабличнуюЧасть(Форма)
	
	Если НЕ ЗначениеЗаполнено(Форма.Интерес) Тогда
		Возврат;
	КонецЕсли;
	
	Объект = Форма.Объект;
	
	Если Объект.Интересы.НайтиСтроки(Новый Структура("Интерес", Форма.Интерес)).Количество() = 0 Тогда
		
		Форма.ЗаблокироватьДанныеФормыДляРедактирования();
		Форма.Модифицированность = Истина;
		
		НоваяСтрока = Объект.Интересы.Добавить();
		НоваяСтрока.Интерес = Форма.Интерес;
		
		Форма.ОбновитьОблакоИнтересов();
		
	КонецЕсли;
	
	Форма.Интерес = Неопределено;
	
КонецПроцедуры

Процедура СформироватьЗаголовокВариантаОтбора(ЭлементВариантОтбора, УстановленоОтборов)
	
	ПозицияНачало = Найти(ЭлементВариантОтбора.Заголовок, " (");
	Если ПозицияНачало <> 0 Тогда
		ЭлементВариантОтбора.Заголовок = Лев(ЭлементВариантОтбора.Заголовок, ПозицияНачало-1);
	КонецЕсли;
	
	Если УстановленоОтборов <> 0 Тогда
		ЭлементВариантОтбора.Заголовок = ЭлементВариантОтбора.Заголовок + " (" + УстановленоОтборов + ")";
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
