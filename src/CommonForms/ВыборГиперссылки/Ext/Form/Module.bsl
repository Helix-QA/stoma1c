
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	// Вибираем значение "http:"
	Протокол = Элементы.Протокол.СписокВыбора.НайтиПоЗначению("http://").Значение;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗакрытьПоOK(Команда)
	Если URL <> "" Тогда
		Закрыть(Протокол + URL);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОткрытиеСправки(Команда)
	
	ЭтаФорма.ОткрытьСправкуФормы();
	
КонецПроцедуры

 #КонецОбласти