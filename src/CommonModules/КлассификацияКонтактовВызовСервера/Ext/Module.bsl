
#Область СлужебныеПроцедурыИФункции

// Функция создания тега
//
// Параметры:
//  НаименованиеТега - строка - имя тега
// Возвращаемое значение:
//  СправочникСсылка.Теги - ссылка на созданный элемент
Функция СоздатьТег(НаименованиеТега) Экспорт
	
	Возврат КлассификацияКонтактов.СоздатьТег(НаименованиеТега);
	
КонецФункции

// Функция получения контагентов по тегам
//
// Параметры:
//  Теги - массив - СправочникСсылка.Теги - теги по которым необходимо получить контрагентов
// Возвращаемое значение:
//  массив - СправочникСсылка.Контрагенты - контрагенты содержащие все заданные теги.
Функция КонтрагентыПоТегам(Теги) Экспорт
	
	Контрагенты = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Контрагенты.Ссылка КАК Контрагент,
		|	Контрагенты.Теги.(
		|		Тег
		|	)
		|ИЗ
		|	Справочник.Контрагенты КАК Контрагенты";
	
	ВыборкаКонтрагенты = Запрос.Выполнить().Выбрать();
	Отбор = Новый Структура("Тег");
	
	Пока ВыборкаКонтрагенты.Следующий() Цикл
		
		ВыборкаТеги = ВыборкаКонтрагенты.Теги.Выбрать();
		ЕстьВсеТеги = Истина;
		
		Для Каждого Тег Из Теги Цикл
			Отбор.Тег = Тег;
			ВыборкаТеги.Сбросить();
			Если НЕ ВыборкаТеги.НайтиСледующий(Отбор) Тогда
				ЕстьВсеТеги = Ложь;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ЕстьВсеТеги Тогда
			Контрагенты.Добавить(ВыборкаКонтрагенты.Контрагент);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Контрагенты;
	
КонецФункции

// Функция получения контагентов по сегментам
//
// Параметры:
//  Сегменты - массив - СправочникСсылка.Сегменты - сегменты для которых необходимо получить контрагентов
// Возвращаемое значение:
//  массив - СправочникСсылка.Контрагенты - контрагенты сегментов.
Функция КонтрагентыСегментов(МассивСегментов) Экспорт
	
	Контрагенты = Новый Массив;
	
	Для Каждого Сегмент Из МассивСегментов Цикл
		
		СоставСегмента = Сегменты.МассивЭлементовСегмента(Сегмент);
		
		Если Контрагенты.Количество() = 0 Тогда
			Контрагенты = СоставСегмента;
		Иначе
			Контрагенты = ОбщегоНазначенияКлиентСервер.ПолучитьСовпадающиеЭлементыМассивов(Контрагенты, СоставСегмента);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Контрагенты;
	
КонецФункции

// Функция получения контагентов по тегам и сегментам. Отборы складываются по логическому "И",
// т.е. отсекаются контрагенты, не удовлетворяющие хотя бы одному заданному отбору.
//
// Параметры:
//  Теги - массив - СправочникСсылка.Теги - теги по которым необходимо получить контрагентов
//  Сегменты - массив - СправочникСсылка.Сегменты - сегменты для которых необходимо получить контрагентов
// Возвращаемое значение:
//  массив - СправочникСсылка.Контрагенты - контрагенты по тегам и сегментам.
Функция КонтрагентыПоТегамИСегментам(Теги, Сегменты) Экспорт
	
	Контрагенты = Новый Массив;
	
	Если Теги.Количество() > 0 И Сегменты.Количество() > 0 Тогда
		Контрагенты = ОбщегоНазначенияКлиентСервер.ПолучитьСовпадающиеЭлементыМассивов(КонтрагентыПоТегам(Теги), КонтрагентыСегментов(Сегменты));
	ИначеЕсли Теги.Количество() > 0 Тогда
		Контрагенты = КонтрагентыПоТегам(Теги);
	ИначеЕсли Сегменты.Количество() > 0 Тогда
		Контрагенты = КонтрагентыСегментов(Сегменты);
	КонецЕсли;
	
	Возврат Контрагенты;
	
КонецФункции

// Функция возвращает соответствие между строковыми представлениями и ссылками на
// соответствующие значения перечисления статусов клиентов
//
// Возвращаемое значение:
//       Соответствие - Ключ:Строка, Значение:ПеречислениеСсылка.СтатусыКлиентов
//
Функция ПолучитьСоответствиеПредставленийСтатусовКлиентов() Экспорт
	
	Соответствие = Новый Соответствие;
	Соответствие.Вставить("Потенциальный"	, Перечисления.СтатусыКонтрагентов.Потенциальный);
	Соответствие.Вставить("Новый"			, Перечисления.СтатусыКонтрагентов.Первичный);
	Соответствие.Вставить("Постоянный"		, Перечисления.СтатусыКонтрагентов.Постоянный);
	Соответствие.Вставить("НеВернувшийся"	, Перечисления.СтатусыКонтрагентов.НеВернувшийся);
	Соответствие.Вставить("Отказ"			, Перечисления.СтатусыКонтрагентов.Отказ);
	
	Возврат Соответствие;
	
КонецФункции

#КонецОбласти
