
#Область СлужебныеПроцедурыИФункции

&НаСервере
// Функция позволяет получить ссылку на существующий для указанной учетной записи эл. почты
// элемент справочника.
//
Функция ПолучитьПодписьПоУчетнойЗаписи(УчетнаяЗапись) Экспорт
	
	Если ТипЗнч(УчетнаяЗапись) <> Тип("СправочникСсылка.УчетныеЗаписиЭлектроннойПочты") Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	ПодписиЭлектронныхПисем.Ссылка
	                      |ИЗ
	                      |	Справочник.ПодписиЭлектронныхПисем КАК ПодписиЭлектронныхПисем
	                      |ГДЕ
	                      |	ПодписиЭлектронныхПисем.УчетнаяЗапись = &УчетнаяЗапись
	                      |	И ПодписиЭлектронныхПисем.ПометкаУдаления = ЛОЖЬ");
						  
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Ссылка;
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти