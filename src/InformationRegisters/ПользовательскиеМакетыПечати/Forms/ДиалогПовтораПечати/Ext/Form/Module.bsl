
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ИзменитьРабочийКаталогПечати(Команда)
	
	ОткрытьФорму("Справочник.ХранилищеШаблонов.Форма.ФормаРедактированияПараметров",,,,,,Новый ОписаниеОповещения("РедактированияПараметровВыборЗавершение", ЭтотОбъект), РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

// Процедура - Шаблон чека выбор завершение
// 
// Параметры:
//  Результат	 - 	 - 
//  Параметры	 - 	 - 
// 
&НаКлиенте
Процедура РедактированияПараметровВыборЗавершение(Результат, Параметры)  Экспорт

	Если ТипЗнч(Результат) = Тип("Строка") Тогда
		КаталогДляСохраненияДанныхПечати = Результат;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПовторитьПечать(Команда)
	Закрыть(КаталогДляСохраненияДанныхПечати);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КаталогДляСохраненияДанныхПечати = УправлениеПечатью.ПолучитьЛокальныйКаталогФайловПечати();
	Элементы.Сообщение.Заголовок = Элементы.Сообщение.Заголовок + Символы.ПС + Параметры.СообщениеОбОшибке;
	
КонецПроцедуры

#КонецОбласти