
#Область ОбработчикиСобытий

// Обработчик заполнения объекта внешними данными
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)	
	// Заполнение на основании структуры данных
	Если ТипЗнч(ДанныеЗаполнения) = Тип("Структура") Тогда
		Если ДанныеЗаполнения.Свойство("Родитель") И НЕ ЗначениеЗаполнено(ПодчиненРеквизиту) Тогда
			ПодчиненРеквизиту = ДанныеЗаполнения.Родитель.ПодчиненРеквизиту;
		КонецЕсли;
		Если ДанныеЗаполнения.Свойство("ПодчиненРеквизиту") И НЕ ЗначениеЗаполнено(ПодчиненРеквизиту) Тогда
			ПодчиненРеквизиту = ДанныеЗаполнения.ПодчиненРеквизиту;
		КонецЕсли;
		Если ДанныеЗаполнения.Свойство("ТекстШаблона") И НЕ ЗначениеЗаполнено(ТекстШаблона) Тогда
			ТекстШаблона = ДанныеЗаполнения.ТекстШаблона;
		КонецЕсли;
		Если ДанныеЗаполнения.Свойство("ПодчиненДиагнозу") И НЕ ЗначениеЗаполнено(ПодчиненДиагнозу) Тогда
			ПодчиненДиагнозу = ДанныеЗаполнения.ПодчиненДиагнозу;
		КонецЕсли;
	КонецЕсли;
	// Заполнение автора
	Если НЕ ЗначениеЗаполнено(Автор) Тогда
		 Автор = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;
КонецПроцедуры

// Обработчик перед записью объекта
Процедура ПередЗаписью(Отказ)	
	Если НЕ ЭтоГруппа Тогда
	     Наименование = ТекстШаблона;// ?(СтрДлина(ТекстШаблона) > 25,Лев(ТекстШаблона,20) + "...",ТекстШаблона);
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
