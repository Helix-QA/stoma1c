
#Область СлужебныйПрограммныйИнтерфейс

// Процедура создает значения по умолчанию справочника "КлассификаторСоциальныхСтатусов"
//
Процедура СоздатьЗначенияПоУмолчанию() Экспорт
	
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000001")) Тогда
		
		ГруппаРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьГруппу();
		ГруппаРаботающие.Наименование = "Работающие граждане:";
		ГруппаРаботающие.Код = "000000001";
		ГруппаРаботающие.Записать();
	КонецЕсли;	
	ГруппаРаботающие = Справочники.КлассификаторСоциальныхСтатусов.НайтиПоНаименованию("Работающие граждане:");
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000011")) Тогда
		ЭлементРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементРаботающие.Наименование = "Работающие, служащие";
		ЭлементРаботающие.КодОИД = "1.1";
		ЭлементРаботающие.Код = "000000011";
		ЭлементРаботающие.Родитель = ГруппаРаботающие;
		ЭлементРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000012")) Тогда
		ЭлементРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементРаботающие.Наименование = "Индивидуальные плательщики (предприниматель)";
		ЭлементРаботающие.КодОИД = "1.2";
		ЭлементРаботающие.Код = "000000012";
		ЭлементРаботающие.Родитель = ГруппаРаботающие;
		ЭлементРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000013")) Тогда
		ЭлементРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементРаботающие.Наименование = "Пенсионеры (работающие)";
		ЭлементРаботающие.КодОИД = "1.3";
		ЭлементРаботающие.Код = "000000013";
		ЭлементРаботающие.Родитель = ГруппаРаботающие;	
		ЭлементРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000014")) Тогда
		ЭлементРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементРаботающие.Наименование = "Военнослужащие";
		ЭлементРаботающие.КодОИД = "1.4";
		ЭлементРаботающие.Код = "000000014";
		ЭлементРаботающие.Родитель = ГруппаРаботающие;	
		ЭлементРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000002")) Тогда
		ГруппаНЕРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьГруппу();
		ГруппаНЕРаботающие.Наименование = "Неработающие граждане:";
		ГруппаНЕРаботающие.Код = "000000002";
		ГруппаНЕРаботающие.Записать();
	КонецЕсли;
	ГруппаНЕРаботающие = Справочники.КлассификаторСоциальныхСтатусов.НайтиПоНаименованию("Неработающие граждане:");
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000021")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Неорганизованные дети";
		ЭлементНеРаботающие.КодОИД = "2.1";
		ЭлементНеРаботающие.Код = "000000021";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000022")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Организованные дети, учащиеся";
		ЭлементНеРаботающие.КодОИД = "2.2";
		ЭлементНеРаботающие.Код = "000000022";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000023")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Студенты";
		ЭлементНеРаботающие.КодОИД = "2.3";
		ЭлементНеРаботающие.Код = "000000023";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000024")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Неработающие";
		ЭлементНеРаботающие.КодОИД = "2.4";
		ЭлементНеРаботающие.Код = "000000024";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000025")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Безработные зарегистрированные";
		ЭлементНеРаботающие.КодОИД = "2.5";
		ЭлементНеРаботающие.Код = "000000025";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000026")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Домохозяйки";
		ЭлементНеРаботающие.КодОИД = "2.6";
		ЭлементНеРаботающие.Код = "000000026";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000027")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Члены семей в/с";
		ЭлементНеРаботающие.КодОИД = "2.7";
		ЭлементНеРаботающие.Код = "000000027";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000028")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Пенсионеры";
		ЭлементНеРаботающие.КодОИД = "2.8";
		ЭлементНеРаботающие.Код = "000000028";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000029")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Пенсионеры-в/с";
		ЭлементНеРаботающие.КодОИД = "2.9";
		ЭлементНеРаботающие.Код = "000000029";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000210")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Пенсионеры по инвалидности";
		ЭлементНеРаботающие.КодОИД = "2.10";
		ЭлементНеРаботающие.Код = "000000210";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000211")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "БОМЖи";
		ЭлементНеРаботающие.КодОИД = "2.11";
		ЭлементНеРаботающие.Код = "000000211";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000212")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Иностранные граждане и лица без гражданства";
		ЭлементНеРаботающие.КодОИД = "2.12";
		ЭлементНеРаботающие.Код = "000000212";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду("000000213")) Тогда
		ЭлементНеРаботающие = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
		ЭлементНеРаботающие.Наименование = "Беженцы";
		ЭлементНеРаботающие.КодОИД = "2.13";
		ЭлементНеРаботающие.Код = "000000213";
		ЭлементНеРаботающие.Родитель = ГруппаНЕРаботающие;	
		ЭлементНеРаботающие.Записать();
	КонецЕсли;
КонецПроцедуры

// Процедура создает значения из справочника oid:1.2.643.2.69.1.1.1.5 ИЭМК "КлассификаторСоциальныхСтатусов"
//
Процедура ЗаполнитьСправочникКлассификаторСоциальныхСтатусов(Параметры) Экспорт
	
	Если НЕ ПривилегированныйРежим() Тогда 
		УстановитьПривилегированныйРежим(Истина);
	КонецЕсли;
	
	Для Каждого Элемент Из Параметры Цикл
		
		КодЭлемента = Формат(Число(СтрЗаменить(Элемент.code, ".", "")), "ЧЦ=9; ЧВН=; ЧГ=0");
		Если ЗначениеЗаполнено(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду(КодЭлемента)) Тогда
			Если НРег(Элемент.display) <> НРег(Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду(КодЭлемента).Наименование) Тогда
				ЭлементСправочника = Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду(КодЭлемента).ПолучитьОбъект();
				ЭлементСправочника.Наименование = Лев(Врег(Лев(Элемент.display,1))+ Нрег(Сред(Элемент.display,2)), 100);
				Если Элемент.code <> "1" И Элемент.code <> "2" Тогда
					ЭлементСправочника.КодОИД = Элемент.code;
				КонецЕсли;
				ЭлементСправочника.Записать();
			КонецЕсли;
		Иначе
			Если Элемент.code <> "1" И Элемент.code <> "2" Тогда
				ЭлементСправочника = Справочники.КлассификаторСоциальныхСтатусов.СоздатьЭлемент();
				ЭлементСправочника.КодОИД = Элемент.code;
				Если Лев(Элемент.code, 1) = "1" Или Лев(Элемент.code, 1) = "2" Тогда
					Родитель = Справочники.КлассификаторСоциальныхСтатусов.НайтиПоКоду(Формат(Число(Лев(Элемент.code, 1)), "ЧЦ=9; ЧВН=; ЧГ=0"));
				Иначе
					Родитель = Справочники.КлассификаторСоциальныхСтатусов.ПустаяСсылка();
				КонецЕсли;
				ЭлементСправочника.Родитель = Родитель;
				ЭлементСправочника.Наименование = Лев(Врег(Лев(Элемент.display,1))+ Нрег(Сред(Элемент.display,2)), 100);
				ЭлементСправочника.Код = КодЭлемента;
				ЭлементСправочника.Записать();
			Иначе
				ГруппаСправочника = Справочники.КлассификаторСоциальныхСтатусов.СоздатьГруппу();
				ГруппаСправочника.Наименование = Лев(Врег(Лев(Элемент.display,1))+ Нрег(Сред(Элемент.display,2)), 100);
				ГруппаСправочника.Код = КодЭлемента;
				ГруппаСправочника.Записать();
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

#КонецОбласти